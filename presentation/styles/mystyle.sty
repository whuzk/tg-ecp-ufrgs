\ProvidesPackage{mystyle}

% ----------------------------------------------------------
% Pacotes fundamentais 
% ----------------------------------------------------------
\usepackage[T1]{fontenc}            % selecao de codigos de fonte
\usepackage[utf8]{inputenc}         % codificacao do documento
\usepackage[brazil]{babel}          % idioma do documento
\usepackage{times}                  % uso da fonta Times
\usepackage{tikz}                   % graficos programaveis
\usepackage{epstopdf}               % uso de figuras no formato eps
\usepackage{booktabs}               % formataçao profissional de tabelas
\usepackage{tabularx,colortbl}      % uso de cores em tabelas
\usetikzlibrary{arrows,shapes}      % uso de formas e setas
\usetikzlibrary{decorations.pathreplacing}

% ----------------------------------------------------------
% Configuracoes de pacotes
% ----------------------------------------------------------
\usefonttheme{serif}
\usecolortheme{seagull}
\useoutertheme{infolines}
\setbeamertemplate{headline}[default]
\setbeamertemplate{navigation symbols}{}
\setbeamertemplate{blocks}[rounded][shadow=true]
\setbeamercovered{transparent}
